# Super Library 📚

**Super Library** — это учебный Spring Boot-проект, представляющий собой онлайн-библиотеку для работника библиотеки.  
Приложение позволяет вести учёт **посетителей** и **книг**, а также **назначать книги конкретным людям**.

Интерфейс выполнен в ламповом и уютном стиле, ориентирован на простоту и наглядность работы.

---

## Возможности

- CRUD-операции для:
  - 📖 книг
  - 👤 посетителей
- Назначение книг посетителям
- Просмотр информации о том:
  - какая книга у кого находится
  - какие книги закреплены за посетителем
- Простая навигация и единый layout
- Серверный рендеринг HTML (Thymeleaf)

---

## Стек технологий

- **Java**
- **Spring Boot**
  - Spring MVC
  - Spring JDBC
- **PostgreSQL**
- **Thymeleaf**
- **HTML / CSS / JavaScript**
- **Maven**

---

## Структура проекта
```yml
.
├── README.md 
├── pom.xml
├── src
│ └── main
│ ├── java
│ │ └── practise
│ │ ├── controllers # MVC-контроллеры
│ │ ├── dao # DAO-слой для работы с БД
│ │ ├── models # Сущности (Book, Person)
│ │ └── SuperLibraryApplication.java
│ └── resources
│ ├── application.yml # Конфигурация приложения
│ ├── schema.sql # SQL-схема БД
│ ├── static # Статика (CSS, JS, изображения)
│ └── templates # HTML-шаблоны (Thymeleaf)
```

---

## Архитектура

Проект построен по классической MVC-модели:

- **Controller** — обработка HTTP-запросов
- **DAO** — работа с базой данных (PostgreSQL)
- **Model** — доменные сущности
- **View** — Thymeleaf-шаблоны

Используется явный DAO-слой без JPA, что делает логику работы с БД более наглядной для обучения.

---

## База данных

- **PostgreSQL**
- Инициализация структуры БД выполняется через `schema.sql`
- Основные сущности:
  - `Person`
  - `Book`
- Связь: книга может быть назначена конкретному посетителю

---

## Запуск проекта (Docker)

### 1. Клонировать репозиторий

```bash
git clone <URL_репозитория>
```
### 2. Перейти в папку проекта
```bash
cd SuperLibrary
```

### 3. Запустить приложение через Docker
```bash
docker compose up --build
```

### 4. Открыть в браузере
```bash
http://localhost:8080
```